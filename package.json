{
  "scripts": {
    "test": "./node_modules/.bin/cypress run --spec 'cypress/tests/test_spec.js' --reporter mocha-allure-reporter",
    "report": "./node_modules/.bin/allure generate allure-results --clean -o allure-report"
  },

  "devDependencies": {
    "cypress": "^9.4.1"
  }
}
